[pytest]
markers =
    integration: marks tests as integration tests
filterwarnings =
    # The mcp library appears to emit an erroneous deprecation warning.
    # We are using `streamable_http_client` as recommended by the warning message,
    # so we are suppressing it here.
    ignore:Use `streamable_http_client` instead.:DeprecationWarning
